<h1>Cart</h1>

<%= form_tag cart_update_path, :method => :post  do %>
<div id="cart">
  <table class="table table-striped" width="600">
    <thead>
      <tr>
        <th>Product Name</th>
        <th>Quantity</th> 
        <th>Price</th> 
        <th>Item Subtotal</th>
        <th>&nbsp;</th>
      </tr>
    </thead>
    <tbody>
    <% @cart.cart_products.each do |cp| %>
      <tr id="cart_item_<%= cp.product.id %>">
        <td><!--<%=image_tag cp.product.thumbnail%>--><%= cp.product.title %></td>
        <td>
          <%= text_field_tag "cp_id_quant[#{cp.id}]", cp.quantity, :size => 4, :class => "quantity" %>
        </td> 
        <td class="cart_item_price"><%= cp.price %></td>
        <td class="cart_item_total"><%= number_to_currency(cp.total) %></td>
        <td><%= link_to "", cart_remove_product_path(:cp_id => cp.id), :class => 'i icon-remove', :method => :post, :confirm => 'Are you sure you\'d like to remove this item?' %>
      </tr>
    <% end %> 
    </tbody>
       <tr>
        <td>&nbsp;</td>
        <td>&nbsp;</td>
        <td class="order-total">Total:</td>
        <td id="order-total"><%= number_to_currency(@cart.total) %></td>
        <td>&nbsp;</td>
      </tr>
  </table>
</div>

<div class="cart-form-actions">
  <%= link_to 'Keep Shopping', store_index_path, :class => 'btn'  %>
  <%= submit_tag "Empty Cart", :class => 'btn', :id => "empty_cart" %>
  <%= submit_tag "Update Cart", :class => 'btn', :id => "update_cart" %>
  <%= link_to 'Checkout', checkout_path, :class => 'btn' %> 
</div>
<% end %>