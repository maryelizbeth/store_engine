<h1>Orders</h1>
<div class="filter">
Filter by Status:
<%= form_tag admin_orders_path, :method => :get  do %>
  <%= select_tag :order_status, options_for_select(@order_statuses, @selected_order_state), :prompt => "all statuses" %>
  <%= submit_tag "Filter", :class => 'btn btn-mini', :id => "submit_category_filter"%>
<% end %>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>User</th>
      <th>Status</th>
      <th>&nbsp;</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.user.full_name %></td>
        <td><%= order.status %></td>
        <td><%= link_to 'View', admin_order_path(order), :class => 'btn btn-mini' %></td>
        <td><%= display_status_transition_links(order) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
